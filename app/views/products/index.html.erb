<h1>Products</h1>

<div>
  <div class="row">
    <% @products.each do |product| %>
      <%= render product %>
    <% end %>
  </div>
</div>
<%= link_to 'New Product', new_product_path %>

<div class="page_bar">
  <%= will_paginate @comments %>
</div>
